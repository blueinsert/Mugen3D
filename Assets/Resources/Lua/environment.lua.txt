local M = {}

local function init(self, char)
    self.char = char
	self.env = CS.Trigger.Instance
end

function M.new(player)
    local obj = {}
	init(obj, char)
	setmetatable(obj, {__index = M})
	return obj
end

function M:AnimElem()
    return self.env:AnimElem(self.char.player)
end


return M